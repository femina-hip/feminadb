<h1>New region</h1>

<%= error_messages_for :region %>

<%= form_for(:region, :url => admin_regions_path) do |f| %>
  <%= render(partial: 'form', locals: { f: f }) %>
  <p><button class="debounced">Create</button></p>
<% end %>

<%= link_to 'Back', admin_regions_path %>
