= render_pretty_header 'Users' do
  %p You may edit the master list of Users here.
  %p If a User has trouble logging in, you may change the User's password.

%table.users
  %thead
    %tr
      %th Login
      %th Email
      %th
      %th
  %tbody
    - @users.each do |user|
      %tr{:class => cycle('odd', 'even')}
        %td= h user.login
        %td= h user.email
        %td= link_to 'Edit', edit_admin_user_path(user)
        %td= button_to 'Destroy', [ :admin, user ], method: :delete, data: { confirm: 'Are you sure you want to delete this User?' }
    %tr
      %td.add{:colspan => 4}
        = link_to 'Create New User', new_admin_user_path
