= render_pretty_header "Waiting Orders for #{@publication.name}" do
  %p
    These
    = glossary_term('Waiting Orders')
    will never become Orders. They only show that the Customers
    %em want
    to have Standing Orders.

  %p
    If you are logged in and authorized, you may click on the listed
    quantities to edit them.

= render('application/customer_search', :path => publication_waiting_orders_url(@publication), :num_results => @waiting_orders.total_entries)

%table.publication-waiting-orders
  %thead
    %tr
      %th Type
      %th Region
      %th District
      %th Customer
      %th Qty
      %th

  %tbody
    - @waiting_orders.each do |waiting_order|
      = render :partial => 'list_item', :locals => { :waiting_order => waiting_order }
= will_paginate @waiting_orders

%p.actions
  = link_to 'Export Data to Excel', publication_waiting_orders_path(@publication, :format => :csv)

%p
  To add a new Waiting Order, go to the
  = link_to 'Customers', customers_url
  page, find the Customer, and add a Waiting Order in that Customer's page.
